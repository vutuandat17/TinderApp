# Tindah Mobile

Expo React Native app for the Tinder-style clone.

```bash
cd frontend
npm install
npm start
```

Use `EXPO_PUBLIC_API_URL=http://YOUR_API_HOST:5000/api` when testing on a physical device.
